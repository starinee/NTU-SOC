Minimum acceptable deployment proxy for the filtered distilled tiny CNN-LSTM. This folder contains a TorchScript export and a per-tensor INT8 weight archive. It is a weight-storage proxy artifact, not physical MCU validation or INT8 runtime inference. The report includes model size, estimated RAM, MACs/FLOPs, and CPU batch-1 latency.
