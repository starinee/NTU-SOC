No existing source files were edited for this experiment. The script was run from temporary workspace. Training data combines the matched 25 °C, 10 °C, and 0 °C training profiles; test profiles remain separated by temperature. The two selected models are Filtered-feature MLP and Filtered CNN-LSTM Teacher, using the same feature definitions and hyperparameters as the strict matched pipeline.\n